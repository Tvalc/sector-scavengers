/**
 * Idle Scene (Re-export)
 *
 * This module re-exports from the refactored idle/ module.
 * Import from './idle' directly for new code.
 *
 * @deprecated Import from './idle' instead
 */

export { IdleScene } from './idle/index';
